// library core
import "./kaboom.js";

export const k = kaboom({
  width: (64*20),
  height: (64*9),
  font: "Arial",
  background: [0, 0, 0],
  // stretch: false,
  // crisp: true
  // letterbox: true
});

export default k
